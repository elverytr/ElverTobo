import React, { Component } from 'react'

export default class Contacto extends Component {
    render(children) {
        return (
            <>
               {this.props.children} 
            </>
        )
    }
}
